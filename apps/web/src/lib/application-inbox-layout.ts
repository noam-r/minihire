/** Shared grid for applications inbox header + rows (must match exactly). */
export const applicationInboxGridClass =
  "lg:grid lg:grid-cols-[minmax(0,1fr)_max-content_max-content_minmax(9.5rem,11rem)_auto] lg:gap-x-4";

export const applicationInboxGridPaddingClass = "px-4";
